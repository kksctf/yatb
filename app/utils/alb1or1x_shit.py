import datetime

# Ну я реально не знаю куда это положить, чтобы избежать `most likely due to a circular import`
# В first_pwned_str он не нужен, так что можно вернуть где было и юзать только в скорборде
