<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>
        {% block title %}{{ CTF_NAME }}{% endblock %}
    </title>
    <script>
        // Set initial theme ASAP to reduce flash
        (function () {
            try {
                var saved = localStorage.getItem('theme');
                var sysDark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
                document.documentElement.dataset.theme = saved || (sysDark ? 'dark' : 'light');
            } catch (_) { /* no-op */ }
        })();
    </script>
    <link rel="stylesheet" href="{{ url_for('static', path='layout.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', path='style.css') }}">
    <!-- Bulma & HTMX from CDNs -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.1/css/bulma.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bulma-toast@2.4.2/dist/bulma-toast.min.js"></script>
    <script src="https://unpkg.com/htmx.org@2.0.4"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css" integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    {# optional per-page extra tags #}
    {% block extra_head %}{% endblock %}
</head>

<body>
    <nav class="navbar" role="navigation" aria-label="main navigation">
        <div class="navbar-brand">
            <a class="navbar-item" href="{{ url_for('index') }}">
                <figure class="image is-24x24">
                    <img src="{{ url_for('static', path='logo.png') }}" />
                </figure>
                {{ CTF_NAME }}
            </a>
        </div>
        <div class="navbar-menu">
            <div class="navbar-start">
                <a class="navbar-item" href="{{ url_for('tasks_page') }}">{{ _("Challenges", request) }}</a>
                <a class="navbar-item" href="{{ url_for('scoreboard_page') }}">{{ _("Scoreboard", request) }}</a>
            </div>
            <div class="navbar-end">
                {% if curr_user %}
                <!-- response placeholder -->
                <div id="flag-submit-result" class="navbar-item"></div>
                <form class="navbar-item" method="post" action="{{ url_for('api_task_submit_flag') }}"
                    hx-post="{{ url_for('api_task_submit_flag') }}" hx-target="#flag-submit-result" hx-swap="innerHTML"
                    hx-on::after-request="if (event.detail && event.detail.successful) window.location.reload()">
                    <div class="field has-addons">
                        <p class="control">
                            <span class="button is-static">{{ FLAG_BASE }}{</span>
                        </p>
                        <p class="control is-expanded">
                            <input class="input" name="flag" placeholder="sampl3_fl4g" required>
                        </p>
                        <p class="control">
                            <span class="button is-static">}</span>
                        </p>
                        <p class="control">
                            <button class="button is-info">{{ _("Submit", request) }}</button>
                        </p>
                    </div>
                </form>
                <a class="navbar-item" href="{{ url_for('profile_page') }}">{{ curr_user.username }}</a>
                {% if curr_user.is_admin %}
                <a class="navbar-item" href="{{ url_for('admin_ng_html_landing', path='') }}">{{ _("Admin area", request) }}</a>
                <!-- before_start = {{ EVENT_START_TIME - NOW() }}, before_end = {{ EVENT_END_TIME - NOW() }} -->
                {% endif %}
                <a class="navbar-item" href="{{ url_for('api_users_logout') }}">
                    {{ _("Logout", request) }}
                </a>
                {% else %}
                <a class="navbar-item" href="{{ url_for('login_page') }}">{{ _("Login", request) }}</a>
                {% endif %}
                <div class="navbar-item">
                    <button id="theme-toggle" class="button is-light" title="Toggle theme" aria-label="Toggle theme">
                        <span class="icon"><i class="fa-solid fa-moon"></i></span>
                    </button>
                </div>
            </div>
        </div>
    </nav>
    <section class="section">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </section>

    {% if curr_user %}
    <script>
        // const evt = new EventSource("/events");
        evt.addEventListener("toast", (e) => {
            const msg = JSON.parse(e.data);
            bulmaToast.toast({
                message: msg.text,
                type: msg.kind === "hint" ? "is-info" : "is-success",
                position: "top-center",
                dismissible: true,
                duration: 5000,
            });
        });
    </script>
    {% endif %}
    <script>
        (function () {
            function applyIcon(theme) {
                var btn = document.getElementById('theme-toggle');
                if (!btn) return;
                btn.classList.remove('is-light', 'is-dark');
                btn.classList.add(theme === 'dark' ? 'is-dark' : 'is-light');
                var icon = btn.querySelector('i');
                if (icon) icon.className = theme === 'dark' ? 'fa-solid fa-sun' : 'fa-solid fa-moon';
            }

            document.addEventListener('DOMContentLoaded', function () {
                var root = document.documentElement;
                applyIcon(root.dataset.theme === 'dark' ? 'dark' : 'light');
                var btn = document.getElementById('theme-toggle');
                if (!btn) return;
                btn.addEventListener('click', function () {
                    var current = root.dataset.theme === 'dark' ? 'dark' : 'light';
                    var next = current === 'dark' ? 'light' : 'dark';
                    root.dataset.theme = next;
                    try { localStorage.setItem('theme', next); } catch (_) { /* ignore */ }
                    applyIcon(next);
                });
            });
        })();
    </script>

{% block footer %}
<footer class="footer" role="contentinfo" aria-label="Site footer">
  <div class="container">
    <nav class="level is-mobile" aria-label="Footer navigation">
      <div class="level-left">
        <div class="level-item">
          <span class="is-size-7 has-text-grey">&copy; 2025 kksctf</span>
        </div>
      </div>

      <div class="level-item has-text-centered">
        <span class="is-size-7">{{ version_string() }}</span>
      </div>

      <div class="level-right">
        <div class="level-item">
          <a href="https://github.com/kksctf/yatb" class="is-size-7" target="_blank" rel="noopener" aria-label="Open project on GitHub">
            <span class="icon is-small" aria-hidden="true" style="vertical-align:-2px;">
              <i class="fa-brands fa-github"></i>
            </span>
            <span>GitHub</span>
          </a>
        </div>
      </div>
    </nav>
  </div>
</footer>
{% endblock %}
</body>

</html>
